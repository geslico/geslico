<%= form_for(@t_contaje) do |f| %>
  <% if @t_contaje.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@t_contaje.errors.count, "error") %> prohibited this t_contaje from being saved:</h2>

      <ul>
      <% @t_contaje.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
